"use client";

export default function MiniTrustLine() {
  return (
    <p className="text-center text-sm text-[var(--text-muted)] px-[var(--container-pad)] py-3">
      Secure payment. Informational only — not medical advice.
    </p>
  );
}
