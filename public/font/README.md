# Fonts for Sleep MVP

**SF-Compact-Rounded-Regular.otf** — used for all text. Bold (headings) is synthesized by the browser from this file, same idea as in the app (design: .rounded + weight: .heavy).
